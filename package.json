{
  "name": "axkit",
  "version": "2.0.0",
  "description": "AxKit UI v2.0 — Modular React UI Kit & Showcase Gallery, powered by SEED Design System v2",
  "scripts": {
    "dev": "npm --prefix docs run dev",
    "build": "npm --prefix docs run build",
    "preview": "npm --prefix docs run preview",
    "lint": "npm --prefix docs run lint"
  },
  "keywords": [
    "axkit",
    "react",
    "ui-kit",
    "design-system",
    "seed-design"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  },
  "peerDependencies": {
    "@seed-design/css": "^2.5.0",
    "@seed-design/react": "^2.3.0",
    "react": ">=18.0.0",
    "react-dom": ">=18.0.0"
  }
}